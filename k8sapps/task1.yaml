apiVersion: v1 
kind: Pod
metadata: # info about Kind 
 name: ashupod1 
spec: # application info like container / volume / security etc
 containers: 
 - name: ashuc1  # name of container 
   image: busybox # image from docker hub 
   command: ["sh","-c","ping fb.com"] # parent process replacement of entrypoint 